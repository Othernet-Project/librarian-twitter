// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var QS_RE, baseUrl, getSection, onTabChange, twitterTabs, win;
  win = $(window);
  baseUrl = window.twitterBaseUrl;
  QS_RE = /section=([^&]+)/;
  onTabChange = function(e, tab, panel) {
    var section, url;
    section = (panel.attr('id')).replace('-tab', '');
    url = baseUrl + section;
    window.history.pushState(null, null, url);
  };
  getSection = function() {
    var match, q;
    q = window.location.search;
    match = QS_RE.exec(q) || [];
    return match[1];
  };
  twitterTabs = $('#tabbable-twitter');
  twitterTabs.tabable();
  twitterTabs.on('tabchange', onTabChange);
  win.on('popstate', function(e) {
    var section;
    section = getSection();
    if (!section) {
      return;
    }
    twitterTabs.tabbableCloseAll();
    twitterTabs.tabbableOpenTab(section + "-tab", true);
  });
})(this, this.jQuery);
